const Loading = (props) => {
  return (
    <div className="centered">
      <img
        className="centered-child"
        src="/images/green-loading.gif"
        style={{
          width: "100px",
        }}
      />
    </div>
  );
};

export default Loading;
